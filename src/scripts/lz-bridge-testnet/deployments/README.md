# Deployments

Per-chain deployment address files (`sepolia.json`, `base-sepolia.json`,
`arbitrum-sepolia.json`) are written here by `LZBridgeTestnetDeploy.s.sol` during `deploy()`
and read back during `configure()`.
