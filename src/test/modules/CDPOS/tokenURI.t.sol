// SPDX-License-Identifier: Unlicensed
pragma solidity 0.8.15;

import {CDPOSTest} from "./CDPOSTest.sol";

contract TokenURITest is CDPOSTest {
    // when the position does not exist
    //  [ ] it reverts
    // [ ] the value is Base64 encoded
    // [ ] the name value is the name of the contract
    // [ ] the symbol value is the symbol of the contract
    // [ ] the position ID attribute is the position ID
    // [ ] the convertible deposit token attribute is the convertible deposit token address
    // [ ] the expiry attribute is the expiry in YYYY-MM-DD format
    // [ ] the remaining deposit attribute is the remaining deposit to 2 decimal places
    // [ ] the conversion price attribute is the conversion price to 2 decimal places
    // [ ] the image value is set
}
