// SPDX-License-Identifier: AGPL-3.0
pragma solidity ^0.8.15;

// AUTOGENERATED BY shell/gen_perms.sh. DO NOT EDIT.
// Run `pnpm run gen:perms` when a module gains or loses a permissioned function.

// Contracts
import {ModuleWithSubmodules} from "src/Submodules.sol";
import {OlympusBoostedLiquidityRegistry} from "src/modules/BLREG/OlympusBoostedLiquidityRegistry.sol";
import {OlympusClearinghouseRegistry} from "src/modules/CHREG/OlympusClearinghouseRegistry.sol";
import {OlympusDepositPositionManager} from "src/modules/DEPOS/OlympusDepositPositionManager.sol";
import {OlympusGovDelegation} from "src/modules/DLGTE/OlympusGovDelegation.sol";
import {OlympusFixedTermLoan} from "src/modules/FLOAN/OlympusFixedTermLoan.sol";
import {OlympusInstructions} from "src/modules/INSTR/OlympusInstructions.sol";
import {OlympusMinter} from "src/modules/MINTR/OlympusMinter.sol";
import {OlympusPrice} from "src/modules/PRICE/OlympusPrice.sol";
import {OlympusPricev1_2} from "src/modules/PRICE/OlympusPrice.v1_2.sol";
import {OlympusPricev2} from "src/modules/PRICE/OlympusPrice.v2.sol";
import {OlympusRange} from "src/modules/RANGE/OlympusRange.sol";
import {OlympusContractRegistry} from "src/modules/RGSTY/OlympusContractRegistry.sol";
import {OlympusRoles} from "src/modules/ROLES/OlympusRoles.sol";
import {OlympusTreasury} from "src/modules/TRSRY/OlympusTreasury.sol";
import {OlympusVotes} from "src/modules/VOTES/OlympusVotes.sol";

/// @notice Selectors of the functions that each module gates with the `permissioned` modifier.
/// @dev    The lists are derived from the compiler AST, which is the only compiler output that
///         records the modifier. Pass one to
///         {ModuleTestFixtureGenerator-generateMultiFunctionFixture} to build a fixture policy
///         that holds every permission the module gates.
library ModulePermissions {
    /// @notice Selectors that {ModuleWithSubmodules} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function moduleWithSubmodules() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](3);
        selectors[0] = ModuleWithSubmodules.installSubmodule.selector;
        selectors[1] = ModuleWithSubmodules.upgradeSubmodule.selector;
        selectors[2] = ModuleWithSubmodules.execOnSubmodule.selector;
    }

    /// @notice Selectors that {OlympusBoostedLiquidityRegistry} gates with the `permissioned`
    ///         modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusBoostedLiquidityRegistry() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](2);
        selectors[0] = OlympusBoostedLiquidityRegistry.addVault.selector;
        selectors[1] = OlympusBoostedLiquidityRegistry.removeVault.selector;
    }

    /// @notice Selectors that {OlympusClearinghouseRegistry} gates with the `permissioned`
    ///         modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusClearinghouseRegistry() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](2);
        selectors[0] = OlympusClearinghouseRegistry.activateClearinghouse.selector;
        selectors[1] = OlympusClearinghouseRegistry.deactivateClearinghouse.selector;
    }

    /// @notice Selectors that {OlympusContractRegistry} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusContractRegistry() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](4);
        selectors[0] = OlympusContractRegistry.registerImmutableContract.selector;
        selectors[1] = OlympusContractRegistry.registerContract.selector;
        selectors[2] = OlympusContractRegistry.updateContract.selector;
        selectors[3] = OlympusContractRegistry.deregisterContract.selector;
    }

    /// @notice Selectors that {OlympusDepositPositionManager} gates with the `permissioned`
    ///         modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusDepositPositionManager() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](5);
        selectors[0] = OlympusDepositPositionManager.mint.selector;
        selectors[1] = OlympusDepositPositionManager.setRemainingDeposit.selector;
        selectors[2] = OlympusDepositPositionManager.setAdditionalData.selector;
        selectors[3] = OlympusDepositPositionManager.split.selector;
        selectors[4] = OlympusDepositPositionManager.setTokenRenderer.selector;
    }

    /// @notice Selectors that {OlympusFixedTermLoan} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusFixedTermLoan() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](15);
        selectors[0] = OlympusFixedTermLoan.createMarket.selector;
        selectors[1] = OlympusFixedTermLoan.setMarketPrincipalCap.selector;
        selectors[2] = OlympusFixedTermLoan.setMarketRiskConfig.selector;
        selectors[3] = OlympusFixedTermLoan.setMarketBaseFee.selector;
        selectors[4] = OlympusFixedTermLoan.setMarketConfigData.selector;
        selectors[5] = OlympusFixedTermLoan.setMarketManager.selector;
        selectors[6] = OlympusFixedTermLoan.setMarketOriginationsEnabled.selector;
        selectors[7] = OlympusFixedTermLoan.setMarketFacility.selector;
        selectors[8] = OlympusFixedTermLoan.createPosition.selector;
        selectors[9] = OlympusFixedTermLoan.addCollateral.selector;
        selectors[10] = OlympusFixedTermLoan.removeCollateral.selector;
        selectors[11] = OlympusFixedTermLoan.increaseDebt.selector;
        selectors[12] = OlympusFixedTermLoan.decreaseDebt.selector;
        selectors[13] = OlympusFixedTermLoan.extendMaturity.selector;
        selectors[14] = OlympusFixedTermLoan.defaultPosition.selector;
    }

    /// @notice Selectors that {OlympusGovDelegation} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusGovDelegation() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](5);
        selectors[0] = OlympusGovDelegation.depositUndelegatedGohm.selector;
        selectors[1] = OlympusGovDelegation.withdrawUndelegatedGohm.selector;
        selectors[2] = OlympusGovDelegation.rescindDelegations.selector;
        selectors[3] = OlympusGovDelegation.applyDelegations.selector;
        selectors[4] = OlympusGovDelegation.setMaxDelegateAddresses.selector;
    }

    /// @notice Selectors that {OlympusInstructions} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusInstructions() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](1);
        selectors[0] = OlympusInstructions.store.selector;
    }

    /// @notice Selectors that {OlympusMinter} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusMinter() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](6);
        selectors[0] = OlympusMinter.mintOhm.selector;
        selectors[1] = OlympusMinter.burnOhm.selector;
        selectors[2] = OlympusMinter.increaseMintApproval.selector;
        selectors[3] = OlympusMinter.decreaseMintApproval.selector;
        selectors[4] = OlympusMinter.deactivate.selector;
        selectors[5] = OlympusMinter.activate.selector;
    }

    /// @notice Selectors that {OlympusPrice} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusPrice() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](6);
        selectors[0] = OlympusPrice.updateMovingAverage.selector;
        selectors[1] = OlympusPrice.initialize.selector;
        selectors[2] = OlympusPrice.changeMovingAverageDuration.selector;
        selectors[3] = OlympusPrice.changeObservationFrequency.selector;
        selectors[4] = OlympusPrice.changeUpdateThresholds.selector;
        selectors[5] = OlympusPrice.changeMinimumTargetPrice.selector;
    }

    /// @notice Selectors that {OlympusPricev1_2} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusPricev1_2() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](2);
        selectors[0] = OlympusPricev1_2.updateMovingAverage.selector;
        selectors[1] = OlympusPricev1_2.changeMinimumTargetPrice.selector;
    }

    /// @notice Selectors that {OlympusPricev2} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusPricev2() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](7);
        selectors[0] = OlympusPricev2.registerNonContractAsset.selector;
        selectors[1] = OlympusPricev2.unregisterNonContractAsset.selector;
        selectors[2] = OlympusPricev2.storeObservation.selector;
        selectors[3] = OlympusPricev2.storeObservations.selector;
        selectors[4] = OlympusPricev2.addAsset.selector;
        selectors[5] = OlympusPricev2.removeAsset.selector;
        selectors[6] = OlympusPricev2.updateAsset.selector;
    }

    /// @notice Selectors that {OlympusRange} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusRange() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](6);
        selectors[0] = OlympusRange.updateCapacity.selector;
        selectors[1] = OlympusRange.updatePrices.selector;
        selectors[2] = OlympusRange.regenerate.selector;
        selectors[3] = OlympusRange.updateMarket.selector;
        selectors[4] = OlympusRange.setSpreads.selector;
        selectors[5] = OlympusRange.setThresholdFactor.selector;
    }

    /// @notice Selectors that {OlympusRoles} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusRoles() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](2);
        selectors[0] = OlympusRoles.saveRole.selector;
        selectors[1] = OlympusRoles.removeRole.selector;
    }

    /// @notice Selectors that {OlympusTreasury} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusTreasury() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](10);
        selectors[0] = OlympusTreasury.increaseWithdrawApproval.selector;
        selectors[1] = OlympusTreasury.decreaseWithdrawApproval.selector;
        selectors[2] = OlympusTreasury.withdrawReserves.selector;
        selectors[3] = OlympusTreasury.increaseDebtorApproval.selector;
        selectors[4] = OlympusTreasury.decreaseDebtorApproval.selector;
        selectors[5] = OlympusTreasury.incurDebt.selector;
        selectors[6] = OlympusTreasury.repayDebt.selector;
        selectors[7] = OlympusTreasury.setDebt.selector;
        selectors[8] = OlympusTreasury.deactivate.selector;
        selectors[9] = OlympusTreasury.activate.selector;
    }

    /// @notice Selectors that {OlympusVotes} gates with the `permissioned` modifier.
    /// @return selectors The selectors, in the order the contract declares them.
    function olympusVotes() internal pure returns (bytes4[] memory selectors) {
        selectors = new bytes4[](7);
        selectors[0] = OlympusVotes.deposit.selector;
        selectors[1] = OlympusVotes.mint.selector;
        selectors[2] = OlympusVotes.withdraw.selector;
        selectors[3] = OlympusVotes.redeem.selector;
        selectors[4] = OlympusVotes.transfer.selector;
        selectors[5] = OlympusVotes.transferFrom.selector;
        selectors[6] = OlympusVotes.resetActionTimestamp.selector;
    }
}
