name: Validate Emergency Config

on:
    push:
        branches:
            - emergency
            - master
            - main
            - develop
        paths:
            - "documentation/emergency/**"
            - "src/scripts/env.json"
            - "shell/validate-emergency-config.js"

    pull_request:
        paths:
            - "documentation/emergency/**"
            - "src/scripts/env.json"
            - "shell/validate-emergency-config.js"

    workflow_dispatch:

jobs:
    validate:
        name: Validate Emergency Config
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

            - name: Bootstrap
              uses: ./.github/actions/bootstrap

            - name: Run validation script
              run: node shell/validate-emergency-config.js

            - name: Check for uncommitted changes
              run: |
                  if [[ -n $(git status --porcelain documentation/emergency/) ]]; then
                    echo "ERROR: Uncommitted changes detected in documentation/emergency/"
                    git status --porcelain documentation/emergency/
                    exit 1
                  fi

    # Uncomment to enable frontend trigger when ready
    # trigger-frontend:
    #   name: Trigger Frontend Rebuild
    #   needs: validate
    #   runs-on: ubuntu-latest
    #   if: github.ref == 'refs/heads/emergency' && github.event_name == 'push'
    #
    #   steps:
    #     - name: Trigger frontend repository_dispatch
    #       uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4.0.1
    #       with:
    #         token: ${{ secrets.FRONTEND_TRIGGER_TOKEN }}
    #         repository: OlympusDAO/olympus-frontend
    #         event-type: emergency-config-updated
    #         client-payload: '{"ref": "${{ github.ref }}", "sha": "${{ github.sha }}"}'